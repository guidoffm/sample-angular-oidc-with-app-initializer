import { Component } from '@angular/core';

@Component({
  selector: 'app-fallback',
  template: `<p>Fallback</p><p class="alert alert-warning"><progress></progress></p>`,
})
export class FallbackComponent {
}
